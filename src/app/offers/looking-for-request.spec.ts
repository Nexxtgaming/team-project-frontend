import { LookingForRequest } from './looking-for-request';

describe('LookingForRequest', () => {
  it('should create an instance', () => {
    expect(new LookingForRequest()).toBeTruthy();
  });
});
